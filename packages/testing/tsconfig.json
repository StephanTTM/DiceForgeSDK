{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM only for the timer functions used to bound a hung presentation;
    // nothing here touches a document, and the kit runs anywhere core does.
    "lib": ["ES2022", "DOM"],
    "types": [],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
